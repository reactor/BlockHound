subprojects {
    group = "io.projectreactor"

    repositories {
        jcenter()
    }

    plugins.withType(MavenPublishPlugin) {
        project.publishing {
            repositories {
                maven {
                    url = uri("$rootProject.buildDir/repo")
                }
            }
        }
    }
}